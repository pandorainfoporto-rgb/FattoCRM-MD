# Defensivos de rede pra evitar ERR_INVALID_THIS / timeouts intermitentes
# (a versão do Node é controlada via engines + Vercel Project Settings,
# não aqui — Vercel rejeita use-node-version no .npmrc)
network-concurrency=4
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
