# pnpm — força Node 20.x mesmo se o Vercel resolver outra versão
use-node-version=20.11.1

# Reduz concorrência da rede pra evitar ERR_INVALID_THIS em alguns hosts
network-concurrency=4
fetch-retries=5
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000
